import * as React from 'react';
import './styles.scss';

export default class ChatPanel extends React.Component<any, any> {
    public render() {
        return(
            <p>Chat Panel</p>
        )
    }
}