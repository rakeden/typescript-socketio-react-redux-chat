import * as React from 'react';
import './styles.scss';

export default class SettingsPanel extends React.Component<any, any> {
    public render() {
        return(
            <p>Settings Panel</p>
        )
    }
}